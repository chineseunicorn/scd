server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      label: master
      server:
        git:
          uri: https://github.com/chineseunicorn/SpringCloudConfig/
          searchPaths: kylin
          username:
          password:

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8889/eureka/